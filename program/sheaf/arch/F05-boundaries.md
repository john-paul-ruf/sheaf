# F05 — Current implementation, proof and remaining boundaries

Reconciled 2026-09-25 from the S06 compaction continuation Final Report (`53f7c73`) and STATE at receive `9d72cf7`, against production `47a633b` plus S06 through `2d8ff2d`. Earlier passes (`5959ca4`, then the continuation-final pass this document previously recorded) remain historical; this revision folds S06's full acceptance into the same coherent description rather than adding a second parallel one (Principle 2). S01/S02/S03/S06 are accepted at 3/6/4/4 checkpoints; S04/S05/S07 remain blocked on human provider registrations (INPUT-DROPBOX/INPUT-ONEDRIVE). No worker or receive was active at this pass. Architecture records evidence; Orchestrator alone owns STATE readiness and dispatch.

## Current agreements and capabilities

| Agreement / capability | Implemented meaning and evidence | Required extension / owner |
| --- | --- | --- |
| CA-34 / CAP-39 | Independent local/vault secrets and recovery scopes, opaque app-key rewrap without rewriting scratch ciphertext; real mounted create/reuse/re-view and J1 accepted. M08/M33/M41/M54, S01 crypto through `8674766`, S02 through `823012b`. | S07 consumes scoped secrets for actual cloud composition; F06 adoption remains separate. |
| CA-35 / CAP-40 | **Current producers and the nonempty V2 extension are both landed and independently verified.** Bounded metadata/read/canonical stream from authenticated pin; parent role supplies scope/kind, frame supplies dimensions/revision. Plaintext reference digest, ciphertext hash, head-body digest and reconstructed authored digest stay distinct. Exporter follows checkpoint/records/events/baselines/source/snapshot descendants and covered chain; vault-only decoder recovers current authored state. M07/M09/M12/M23/M24/M27/M33; `c7e6507`, `7ee5ce8`, `823012b` (current), S06 CP1–CP4 `084ecf9`(+`7e785e4`)/`250c9e0`(+`fccc345`)/`7a7fce8`(+`696824e`/`8a215d0`)/`2d8ff2d` (nonempty V2). | S07 consumes the V2 graph for actual cloud composition; F06 adoption remains separate. No further local producer work is required for F05. |
| CA-36 / CAP-41 local/bundle | `readAppDurability` authenticates app head and app-scoped receipt. Pending count sums uncovered commits across every locally held device frontier; absent/ahead receipt entries reject. `2cd5ad4` corrected 1-versus-3 undercount. Library/app/Settings/detail/reset share facts; stale reset confirmation refuses. Newer edits preserve old time and remain pending. **S06 CP2 (`250c9e0`) proved compaction itself changes neither the pending count nor the confirmed time** — only a subsequent real mutation/receipt does. | S07 extends status to actual configured cloud receipts/failure remedies and proves J4; current local acceptance does not pre-verify that work. |
| CA-37 / CAP-39/42 | DEC-72 approved `27e9a34`: verified download stays unconfirmed until explicit action on the same live operation; native write+close is a distinct saved predicate. Captured worker frontier/artifact and ClockPort time alone authorize atomic receipt/pin release. DEC-71's first authored trigger and 10m/1h/24h/daily dismissal progression persist encrypted; queries/dismissals use current app/home/trigger/count and worker time. `59caafb` prevents unchanged schema/chart commits, preserving stale/validation guards and old history. J1/J2 accepted through `47a633b`. | No save/reminder policy, producer or local proof gap remains. Native OS durability is unqualified. |
| CA-38 / CAP-43 | Shared immutable-object/exact-byte CAS protocol and stateful double are implemented. A fixture receipt is not vendor qualification. | External supplier provides public registrations, exact SPA redirects and authorized accounts; S04 Dropbox/S05 OneDrive CP0–3 implement and qualify account isolation, scopes, CORS and final-publication CAS. S07 consumes qualified adapters. No silent overwrite or scope widening is authorized. |
| CA-39 / CAP-45 | Dedicated ciphertext IO/byte-free page control and lock/cancel lifetimes are implemented for bundles. Real-browser quota refusal is proven **component-level only** (Chromium ignores the CDP quota override) — final-report debt, not a producer gap. | S07 CP1 owns real config/token/OAuth/fetch/CSP wiring and credential-free J6 discovery, probes and negative controls; CP3 scheduler/races, CP5 rerun plus J4/J5. Missing live accounts do not waive local J6. |
| CA-40 / CAP-05 | Worker-authoritative recovery delay is rendered as an ephemeral deadline countdown, with draft/timer cleanup and early-submit suppression; expiry permits only a new request. `f978eb3`, current real-route proof. Vault/save/reminder design accepted `58bffc8`/`4c31ded`; provider states accepted `98ee79c`. **S06 CP2 (`250c9e0`/`fccc345`) landed the retained-history VM/screen** (`M37-view-models.md`/`M44-ui-records.md`), verified at 320/600/900/1200 with axe checked in J3. | No known design-fill blocker remains. S07 CP2/4 consumes provider designs. No S06 work remains — history/restore presentation is landed. |
| CA-41 / CAP-44 | **Landed and independently verified.** DB/Author exact authenticated retained refs/conflict/audit/covered-chain layout (`afe37f3`+`91ad694`+`17a73ae`+`8c303fd`) is implemented: discriminated `AppHeadV1\|V2`/`RecordPageV1\|V2` roots, V2 traversal/original-chain reconstruction (M33 `backup-graph.ts`), the one worker-owned compaction scheduler (128 post-checkpoint commits / 1500ms), retained-history restore and V2 edit+append continuity, vault-only oracle equivalence for every artifact across two real periodic compactions (127/V1 → 128/V1 → V2 cp129 → V2 cp257). S06 CP1–CP4 `084ecf9`(+`7e785e4`)/`250c9e0`(+`fccc345`)/`7a7fce8`(+`696824e`/`8a215d0`)/`2d8ff2d`. | S07 consumes the compacted V2 graph for cloud composition (CA-35 row). No further S06-owned work remains for F05. |

## Sources, actual composition and proof limits

Approved database sections LocalCatalog/HomeState/AppHead/Retention/VaultIndex/AppManifest and migrations 003/004/006 govern durable meaning; user approval chose a graph-contract direction, which DB/Author then supplied in full (`afe37f3`+`91ad694`+`17a73ae`+`8c303fd`) and S06 implemented against. **The nonempty local retained/conflict/audit branches `backup-graph.ts` used to reject are now the V2 producer/reader S06 landed** — the V1-only rejection this section previously described is closed for supported roots; only genuinely unsupported/malformed roots still reject, and that refusal is deliberate (CP1's intermittent counterexample confirmed this: the refusal was correct, the test fixture was not, and the check was not loosened — closed `7e785e4`). No protected edit occurred in this continuation.

The implemented save path is mounted route → workflow/runtime → dedicated data/IO ports → pinned authenticated graph → verified ciphertext delivery → native or explicit correlated completion → encrypted receipt → reopened reader. `AppSessionRegistry.open` coalesces concurrent hydration and rejects late results after close/lock; overlapping visit writes coalesce. `823012b` closed the r9 required-gate counterexample, with failing-before deterministic tests and real import/artifact proof. Historical passing runs did not waive it.

The compaction path is committed authenticated head/frontier → bounded checkpoint/audit export → independent replay equivalence → compare-revision pointer swap → retained-root-aware cleanup (reachability-based, not age-based — `DEC-74`) → worker restart/hydrate/history/restore → new commit → subsequent bundle. A save is acknowledged before background compaction starts; compaction itself never advances receipts/counts (CA-36, above). This is proven through the real worker-owned periodic scheduler and a fresh production build, not a forced/injected trigger.

The reminder path is real authored command → atomic event/catalog trigger → route acknowledgement/query → M05 policy over worker ClockPort → encrypted operational dismissal → reopened query. Import/reconciliation commits count toward backup but do not trigger reminders. The sampled schema/chart no-op tests check no writes/count/history/reminder before and after reopen; invalid and stale requests still refuse. No new source-backed counterexample was established by this pass's bounded samples; that is not a substitute for worker proof or an exhaustive audit.

Independent evidence at `2d8ff2d` (this continuation): `pnpm typecheck` 0, `pnpm lint` 0; candidate+installed selector 39 files/473 pass (with `tests/unit/queries/records.test.ts` substituted for the non-existent `tests/unit/queries/history.test.ts` — a planning-defect selector correction, recorded); full `pnpm test` 229 files/2520 pass/3 inherited skips; Playwright port 8081 fresh build: compaction-backup 2/2 (J3), sync/compaction + sync/bundle 3/3, bundle-backup/append-import/backup-status/records-crud/gate-f02-demo/worker/import-journey 18/18 — 23/23 total; built main bundle carries the `2d8ff2d` build id. CP1 was also independently rechecked in a clean detached worktree at `084ecf9` before the counterexample was closed. This supersedes and extends the `47a633b` S01–S03 evidence below, which remains valid for its own scope: typecheck/lint exit 0, 223 unit files / 2461 passed / 3 inherited skips, and a fresh-build 11-test browser/e2e gate with zero retries/skips. Exact selector: `bundle-backup recovery-countdown sync/bundle scratch-reminders backup-status`, both e2e/browser projects, one worker and 900000ms global bound. Coder's full 81-test e2e result at `47a633b` is source-identical and its digests were checked by root; root did not repeat that slow full suite. Archivist reran no code tests. No OS-native durability, live provider qualification, future cloud/J6 security proof or GATE-F05 human verdict is claimed.

## Current-summary drift returned to Orchestrator

The prior pass's machine-actionable corrections (STATE Decisions DEC-71/72, approval-review disposition, VB-02, CAP-39 action trace, CA-36/37/40 prose, Seam Preflight and graph-resumption summaries) were **resolved** at the `3ea3606` receive — re-inspected this pass and confirmed current: DEC-71/72, the approval-review disposition, CA-37/CA-40's prose and the Seam Preflight table all now correctly cite `47a633b`/`823012b`/`2d8ff2d` and accepted producers, with no lingering "planned"/"missing" wording for landed local work. **No new present-tense drift was found this pass** — the Capability Readiness (CAP-44 verified), Contract Agreements (CA-35/36/40/41 all "agreed" citing `2d8ff2d`) and Current Blockers (PLAN-REVIEW-GRAPH-APPROVED/GRAPH-CONTRACT rows both read "cleared"/"closed", correctly past tense) sections of STATE at `9d72cf7` already agree with the accepted S06 producers and proofs. This is itself worth recording: the receive-time correction discipline held for a second consecutive receive with no recurrence of the stale-prose pattern (see `ARCHIVIST-LOG.md`).

## Carried follow-ups and owners

| Obligation | Current disposition / owner and resumption |
| --- | --- |
| Nonempty graph contract | **Closed.** Implemented and independently verified (CA-35/41, above). S07 consumes the V2 graph for cloud composition. |
| Provider credentials/qualification | External supplier → S04/S05 → S07; accepted provider design `98ee79c` needs no repeat fill. |
| Cloud composition and local security | S07 J4/J5/J6 remain required; J6 is credential-free, not waived by external unavailability. |
| Crash-retained pins and existing-home adoption | Current worker export/release is conservative and implemented; F06 Planner owns user-facing recovery/adoption continuation. No blind cleanup authority. |
| Empty-library unavailable copy | F06 Planner/M42 owns remaining global connection/adoption entry; current per-app backup route works. |
| Preserved-value wording | Closed in S03 `d8b4e14` from actual projected provenance; legacy absent provenance retains existing import wording. |
| 320px toolbar | Closed `47a633b`, original full integration gate retained. |
| Inherited 600px Settings / 900px rail branding | Next Planner UI-maintenance assignment for M46 `schema.module.css` and M39 `app-shell.module.css`; no cleanup source edit here. |
| Export surface and bundle size | CL-03/05 classification brief to Planner remains unaccepted; CL-06 measurement to Planner performance, S07 release verification. Large-chunk warning is not measured tree-shaking failure. |
| Native OS picker/durability | S07 release qualification; fixture results are not that evidence. |
| Real-browser quota refusal | Component-level QuotaExceededError proof accepted for S06 CP3 (Chromium ignores the CDP quota override); carried to S07's J6/security harness if a working browser quota mechanism exists there. |
| `isBackupHeadPinned` performance | Not a defect: exports the full graphs of other roots and pins on every edit. Carried to S07/F06 owner review for a measure-first look, not a required correction here. |
| AR-1 transcript grammar validation | Orchestrator/Archivist validation debt until referenced `program/demiurge/specs/database.md` becomes available; no inferred success. |
| GATE-F05 | Orchestrator prepares completed built demo package, then human verdict; not supplied by general approval. S04/S05/S07 remain the last blockers. |

The report records nine unplanned planning/ownership corrections across the whole F05 cycle to date: the initial preflight, three S02 mechanical lease amendments (r1–r4), the graph split replan, the reminder-clock replan, the S03 compatibility/no-op amendments, and three S06 CP1 lease amendments (r2–r4, before the DB/Author-owned r5–r8 seams). A context terminal (S06 r6) and a provider usage-limit terminal (S06 r8) are environment events, not planning defects; CP1's held-then-corrected counterexample (r8→r9) is a verification gate working correctly, not a planning omission. Convention recurrence and the complete standing recommendations table are recorded in `ARCHIVIST-LOG.md`; exact promoted wording is in [PROGRAM-CONVENTIONS.md](PROGRAM-CONVENTIONS.md) because PROGRAM-CONFIG is protected.
