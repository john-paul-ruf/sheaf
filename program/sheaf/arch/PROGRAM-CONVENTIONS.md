# Program conventions carried from F05 evidence

These conventions are recorded on an Archivist-owned architecture surface at the F05 initial, continuation-final and S06-compaction-final passes (2026-09-24/25). The user's envelope prohibits editing or committing externally modified PROGRAM-CONFIG.md. Its owner can incorporate the exact wording below into Conventions; this pass applies the crossed-threshold promotion here rather than waiting for a completed F05. Verification commands, scheduling and protected format contracts are unchanged.

## Trace the whole bounded backup path before a lease amendment

> Before dispatching or redispatching a backup/retention checkpoint, enumerate all head-mutating writers (including import append), the graph exporter, shared publication/frontier consumers, incremental hash and canonical-encoding primitives, lifecycle owners, and their paired tests/fixtures. Compare boundedness end to end, including pin/load and publication rereads. Batch all visible mechanical path additions into one controlled lease revision. Record unsupported graph branches with both an author-contract supplier and the producer/reader/proof checkpoint that will consume it. Do not use a successful partial producer gate as acceptance of the whole capability.

Threshold crossed on the **in-cycle axis**: F05 S02 r1 found the CSV append retention writer/test seam (`9466660` amendment); r2 found eager shared graph/publication/frontier and fixture seams (`760dae1`); r3 found missing incremental hash/CBOR primitives/tests (`3093131`). A fourth return, r4, exposed nonempty graph source/mapping ownership and led to `f1eae46`. Three mechanical lease corrections plus a semantic replan are distinct instances, not one observation because they share a MASTER. The first three alone cross the bar. The fourth requires DB/Author input, not mechanical invention of wire semantics.

This specializes existing framework preflight and owner-recheck rules; it does not create a new review role or demand a new approval. DEC-71/72 are independent product decisions. Native await timeout reattachment and the r5→r6 continuation of independently safe native work are not counted as additional lease defects. The later r7 context terminal and r8 execution shortfall are separately recorded, not folded into prerequisite counts.

## Include authored command families and their actual proof prerequisites

> When a capability promises no-op suppression, post-acknowledgement behavior or time-based persistence, trace every accepted mutation family through its command producer, atomic durable write, response mapping and mounted consumer. Lease stale/invalid/no-change guards and all affected fixtures/specs with the correction. Name the actual authority realm and executable clock/control mechanism, its behavioral self-tests, restart isolation and artifact identity before the first dependent proof. Adapt existing journeys at their accepted-action boundaries without suppressing the new behavior globally or dropping their assertions. Batch all visible prerequisites before redispatch.

This is an extension of the same complete-prerequisite-trace convention and standing ID `941f0fdbdd02f2c4`, not a new review stage. The continuation independently crosses the **three in-cycle corrections** bar: reminder-clock replan `37303ce` assigns the actual worker realm/harness/self-tests; compatibility amendment `50aed81` leases five affected old-journey specs; no-op amendment `0634e89` leases six command/consumer/test paths before S03 resumes. Together with S02 r1–r4, this is **1 cycle, 7 specific prerequisite corrections**; the report's eight total also includes initial preflight. No elapsed-time or worker-shortfall event is counted as a planning omission. Promote now; protected PROGRAM-CONFIG incorporation remains its owner's action.

## S06 compaction adds three more mechanical lease-seam instances (2026-09-25)

Standing ID `941f0fdbdd02f2c4` recurred again within the same F05 cycle, independently of the S02/S03 evidence above: **S06 CP1's own first three attempts (r2, r3, r4) each found a lease-completeness gap before any implementation checkpoint landed**, the identical shape as S02 r1–r3.

- **r2** — a scoped Archivist review (`Adw4M`) found `src/persistence/codecs/vault.ts` and its paired test missing from S06's lease: the vault manifest codec rejects reference arrays unless physically ordered, while S06's compaction candidate needed logical audit/conflict order, and neither the exporter/publication assignment nor S06's 66-path lease covered the manifest codec's own encode/decode boundary. Corrected by lease amendment (`baf4b56`).
- **r3** — the session's own return found a "history-copy" consumer seam: a copy/consumer path the CP1 candidate needed was not in the lease. Corrected by amendment (STATE "SESSION-06 r3 receive").
- **r4** — partial CP1 preserved with zero commits; an exact runtime fixture (`tests/browser/worker/runtime.ts`) was added under a further lease amendment (instruction r5) before implementation could proceed to a committed checkpoint.

These three are **mechanical lease-completeness gaps** in the same sense as S02's — a missing consumer/producer/fixture discovered only once implementation began, not a product or DB/Author input. They are counted separately from S06's later r5/r7 "missing Author mapping" returns and r8's provider-usage-limit terminal, which are **not** counted here: `DEC-75` explicitly designates DB/Author re-entry as the expected mechanism for discovering a new durable-constraint detail during implementation of a deliberately incremental graph-contract format, and a provider capacity limit is an environment event, not a planning defect. Conflating either with a lease-completeness gap would overstate this pattern; they are recorded as their own observation below instead.

Updated count for `941f0fdbdd02f2c4`: **1 cycle (F05), 10 in-cycle mechanical-prerequisite instances** (S02 r1–r4: 4; continuation clock/compatibility/no-op: 3; S06 CP1 r2–r4: 3). The bar was already crossed at 4; this is confirmation the pattern recurs at the start of *every* new working set within the cycle, not a new promotion decision. The Final Report's own granularity feedback additionally flags CP1 as a single very-large (44-file) checkpoint for a future graph-contract session to split (codec/candidate vs. evidence replay) — recorded as supporting context for why a large first checkpoint in a new working set accumulates repeated lease-completeness gaps, not as a separate standing pattern.
