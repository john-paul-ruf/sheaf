# Archivist log

Historical entries remain append-only in `ROSHI-LOG.md` (F01–F04, latest `60a6a81`). No prior ARCHIVIST-LOG.md existed at this pass. This log continues that record and preserves its standing IDs verbatim, including adopted/retired rows for traceability.

## 2026-09-24 — durable-home-backup (F05), final, BLOCKED / INCOMPLETE

### Record and scope

Read `program-agents/ARCHIVIST.md` in full, the full F05 Final Report and STATE (all raw returns), all architecture files including pre-existing untracked seeds, PROGRAM-CONFIG, all seven sessions and MASTER, original planning/graph review results, git history and the last several historical log entries (F02/F03/F04 final). Inspected affected implementation and sampled behavioral tests against approved database/format meaning. No source, tests, specifications, migration, role, STATE, MASTER, SESSION or report was edited. No tests or live browser/provider/OS-save proofs were rerun by Archivist.

Implementation ends at `d75830d`; root receive `95a539d`; consumed arch deltas `5d73ba2`, `c25742f`, `5a977e6`, `385e794`; graph replan `f1eae46`. Root report records `pnpm verify`: typecheck/lint/build exit 0, 214 files / 2402 passing / 3 inherited skips. Current graph gate: 25 files / 209 independently passing. Native selected 19 files / 106 passing is worker-reported and included in root's full suite. These results do not establish a completed F05 capability.

### Reconciled drift

- Folded F05 deltas into current M01/M07/M08/M09/M12/M23/M24/M27/M32/M33/M34/M51/M53/M56/M59/M62 contracts, ahead of preserved change history. Replaced the S01 eager graph bytes and semantic buffer description with the `c7e6507` metadata/bounded readers and owned lifetime. Native `d75830d` receipt/IO contribution is explicitly partial.
- Replaced planned-only heads in M24/M27/M59/M62 with their landed subsets. M33 now names the real IO worker; export worker remains future. M32 distinguishes byte-free page RPC from the separate IO channel. Updated append head retention, streamed projection/replay/provenance and bootstrap/file-save facts. Valid non-saved completions and aborted transfers have different pin outcomes; CP6 recovery remains open.
- M56's combined M56/M57 S01 material is now unit evidence in M56 and `tests/property/sync/vault.test.ts` evidence in new M57. Property round trips are not storage/restart or provider proof.
- Corrected two inherited documentation remnants: M15 no longer suggests zip.js is used (native M13 reader shipped); M19 no longer names removed `inferProposal`. M36 now names S02 CP5 as the unfinished inherited CAP-05 countdown owner.
- Created `arch/MODULE-REGISTRY.md` from TypeScript AST scans of every registered module's non-test source: type-only imports/exports excluded, value re-exports and dynamic imports included, worker URL construction separate. M24/M27/M59/M62 exist; seeds M05/M25/M26/M29/M30/M47/M64 remain future and byte-preserved. Module edges were not inferred from symbol searches. Config's historical/planned registry remains unchanged under the explicit protected-owner instruction.
- `arch/F05-boundaries.md` reconciles CA-34–41, current graph compatibility scope, pending readers and author inputs with source/proof/owner references. It does not mark STATE ready or grant dispatch.

### Current blockers and owner corrections

S01 3/3; S02 2/6 plus partial CP3; S03–S07 blocked. No J1, complete CAP-39–45, actual OS save, live provider qualification or GATE-F05. Receipt production is present while `app-session.ts` / `record-handlers.ts` still expose absolute local sequence counts; S02 CP4/5 owns receipt-relative readers and exact restart/count proof. S02 CP5 also owns the inherited CAP-05 recovery countdown. Native invalid/aborted pin recovery and complete vault-only artifact recovery remain S02 CP6.

Human DEC-72 → Designer's remaining outcome states → S02 CP3 completion/CP4 J1; human DEC-71 → Designer/S03 reminder policy. `58bffc8` already supplied the fixed named-vault/recovery design portion; its 80 renders are design evidence only. Human/DB Author GRAPH-CONTRACT → S06 CP1 nonempty retained/conflict/audit writers/readers/publication, CP2 installation/history/chain and CP4 J3/J1 regression. Provider registrations, exact redirects and authorized accounts → S04/S05 qualification → S07 real config/token/egress/scheduler and J4–6. These are existing owners, not new plans or product decisions by Archivist.

Machine-actionable record correction for Orchestrator: STATE's Inspected inheritance and Seam Preflight retain present-tense missing-home/IO/save claims superseded by `03ee571`/`d75830d`; current CA/CAP rows are more accurate. Qualify those summaries by revision or refresh them without rewriting historical returns. Narrow DF-F05-1 summary to the remaining decision-dependent design; do not redo the accepted fixed portion. No new source-backed counterexample overturning accepted current-graph evidence was established in the sampled review. Future nonempty and composed proof gaps remain explicitly unverified. AR-1's absent `program/demiurge/specs/database.md` transcript source remains Orchestrator/Archivist validation debt, not inferred format success.

### Recurrence and convention promotion

The threshold is crossed **within one cycle**, not deferred because F05 is incomplete. S02 r1: append retention writer/test omitted → `9466660`; r2: eager shared graph/publication/frontier/fixtures → `760dae1`; r3: incremental hash/CBOR primitives/tests omitted → `3093131`. Those three distinct mechanical returns suffice. r4 adds the graph-contract/producer/reader ownership return → replan `f1eae46`. Three lease amendments and two replans (initial five planning findings at `5e38ca7`, then graph replan) are planning evidence; the product choices themselves are not decomposition defects. No context exhaustion or re-slice occurred. r5→r6 continued independently safe native work in the same context; Native await timeouts were transport reattachments, not worker crashes.

Exact promoted wording, recorded now in owned `arch/PROGRAM-CONVENTIONS.md`:

> Before dispatching or redispatching a backup/retention checkpoint, enumerate all head-mutating writers (including import append), the graph exporter, shared publication/frontier consumers, incremental hash and canonical-encoding primitives, lifecycle owners, and their paired tests/fixtures. Compare boundedness end to end, including pin/load and publication rereads. Batch all visible mechanical path additions into one controlled lease revision. Record unsupported graph branches with both an author-contract supplier and the producer/reader/proof checkpoint that will consume it. Do not use a successful partial producer gate as acceptance of the whole capability.

PROGRAM-CONFIG.md and ROADMAP.md contain externally owned changes and the envelope expressly prohibits their edit/stage/commit. They were preserved; convention incorporation in PROGRAM-CONFIG remains its owner's action, not a deferred decision to recognize the met threshold. Verification commands and scheduling were not changed.

### Proposed for the framework

- **Boundedness is a producer-consumer agreement, not a helper's property:** recommend making the existing mechanical preflight enumerate payload memory, streaming hash/codec consumers and lifetime/retention owners when a checkpoint promises bounded processing. F05 S02 r1/r2/r3/r4: **1 cycle, 4 returns**, of which three are mechanical lease omissions. The specialization above supplies the program rule now. Existing general seam rules are present; this recommends a concrete checklist improvement, not another review stage.
- **Keep present-tense summaries synchronized with received producer facts:** extend the existing CA/CAP receive invariant to Inspected inheritance and Seam Preflight, or explicitly label those sections historical. **1 cycle, 1 receive observation, 2 stale summary locations**. Native IO/home producers landed, while the final summaries still say missing. Do not wait for three cycles to make this recommendation.
- Carry forward the open recommendations in the table below. Size-only interim cadence recurs at **5 cycles**, with **3 F05 seam examples** (eager graph M07, planned M24 head, missing IO M33) added to prior counts. This cycle's two scoped planning reviews mitigated planning gaps; they did not perform final-style synthesis. The framework cleanup-ledger envelope omission remains open but **did not recur in the supplied F05 envelope**, which explicitly fixed its write set.

### Adoption check

Read current PLANNER/CODER/UI-CODER/ORCHESTRATOR text for each carried open item's substance and checked `git log --` plus `git ls-files --` for those exact role paths. They are not tracked in this repository; **there is no attributable role commit to cite**. This limits dating, not the ability to inspect adoption. No inference about human action was drawn from Archivist leaving files unchanged.

`1f54dec2254e8f15` is **adopted** by current PLANNER's “Contract changes include their affected consumers” (production callers, paired fixtures/tests, exact paths and serialized handoff) and shared-assertion ownership rule. It is no longer an open request. Existing receive status invariant (`b06241fe8526baa2`), verification maintenance and shared-index dispositions remain satisfied/retired; `26e3bdf9a1e6dd6f` is normalized from historical “not proposed — already present” to adopted without changing its ID.

Still absent in the reviewed current rules: table-cell validation, an explicit route for an unleased module's own fragment, a live sibling rule-notification channel, exact seed-function requirement and exact-count worker assertion requirement. Generic seed/domain-effect rules are useful but do not fully adopt those narrower requests. The ORCHESTRATOR template still omits cleanup-ledger path and retains the 16-session interim threshold. Counts are held when no new occurrence was established; silence or repeated copying does not increment evidence.

### Cleanup and verification

Created `CLEANUP-LEDGER.md` by reconciling historical CL-01–06. CL-01/04 remain retired on current package/source evidence; CL-02/06 track. Fresh AST export enumeration and whole-word src/tests search finds 211 exported values without outside-file textual references; internal uses and future/dynamic/public contracts prevent calling them dead code. CL-03/05 are briefed, correcting the historical tracking-table/open-brief inconsistency. The standing **Prove or name every unconsumed export** classification brief is refreshed under the repeated-ledger-pattern threshold; it is not removal authority or an approved cleanup program.

Re-read owned diff and new documents, checked links/source paths and `git diff --check`; verified protected config/roadmap hashes against the supplied preservation record, and role/pre-existing seed hashes against startup capture. No implementation suite applies to this prose-only pass. Unrelated modified config/roadmap and untracked seed files remain outside the explicit commit pathspec; a globally clean tree is neither possible nor claimed.

### Standing recommendations

| id | pattern | cycles | in-cycle instances | first seen | status |
| --- | --- | ---: | ---: | --- | --- |
| `27bf4a350ec2f23c` | A dependency must-not ships as a test, not a review note | 4 | 5(F01)+3(F02)+5(F03)+3(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `33dba11758afa4e2` | Encode a must-not as a type or a runtime throw | 4 | 5(F01)+8(F02)+3(F03)+2(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `3e30962a53a57c2f` | Test filters take no bare `--` | 1 | 3 | F01 | promoted → PROGRAM-CONFIG Conventions; no F02/F03/F04 instance |
| `f279893d1ad0a40d` | `Owns` authoritative, Files table indicative | 4 | 7(F01)+9(F02)+12+(F03)+1(F04) | F01 | promoted → Custom Rule 7; F04's continuing mirror-direction instance recorded |
| `3ddf907567cd76e6` | Arch fragment head contract superseded by its own delta | 5 | 4(F01)+6(F02)+6(F03)+1(F04)+6(F05) | F01 | promoted — existing fragment policy; F05 M07/M24/M27/M33/M59/M62 reconciled |
| `b73d9bab7c196038` | Lease or boundary sweep drawn over the wrong tree | 2 | 3(F02) | F01 | promoted at F02 interim → PROGRAM-CONFIG Conventions; held — 0 new instances F03 or F04 |
| `d7d81c3b2cf8e9c9` | Closed union and its exhaustive map split across leases | 2 | 3(F02)+0(F03)+4(F04) | F02 | promoted → PROGRAM-CONFIG Conventions; F04 mixed result — one blocked (S04), three pre-empted after (S05, S06, S08) |
| `f823e9a910b0a671` | A delta describing another module | 4 | 4(F01)+4(F02)+0(F03)+1(F04)+1(F05) | F01 | promoted — existing fragment policy; M56/M57 material separated |
| `442cb40af6e1a830` | No interim drift check scheduled below 16 sessions | 5 | 4(F01)+10(F02)+5(F03)+2(F04)+3(F05) | F01 | open — cadence still size-based; F05 three named stale seams, mitigated by two scoped planning reviews |
| `f1442f23b7f0949d` | Archivist envelope write set omits the cleanup ledger it is asked to own | 4 | 1(F01)+2(F02)+1(F03)+1(F04) | F01 | open — framework template still omits ledger; this F05 envelope explicitly includes it, no new instance |
| `b06241fe8526baa2` | Landed proof legs left in STATE's `planned:` list at receive | 1 | 13 | F02 | **adopted** — ORCHESTRATOR.md's receive step 7 CA/CAP status invariant; F04 shows zero recurrence for the second cycle running |
| `b027c25ef168decb` | STATE table rows that lose a cell go unnoticed | 1 | 6(F02) | F02 | open — no new F05 malformed-row finding; current role lacks a table-shape check |
| `06e04ece038e736d` | A module no session leased has no route into its own fragment | 1 | 6(F02) | F02 | open — no new F05 instance; generic fragment integration does not specify unleased-module routing |
| `3261d808a9c412da` | FORGE-CONFIG Verification Commands has no post-baseline maintainer | 1 | 2 | F01 | adopted (rule + supersession text exist); third consecutive cycle of the expected maintenance note, not a recurrence of the original gap |
| `1a8974acd0d1350e` | Checkpoint boundary blur: capability bodies and proofs split across checkpoints | 2 | 2(F01)+0(F02) | F01 | retired — settled negative at F02; no F03 or F04 instance either (32+31+35 checkpoints across three cycles, no sizing failure) |
| `26e3bdf9a1e6dd6f` | Authorized contract change breaks a consumer outside the lease | 1 | 1 | F02 | adopted — substance already in PLANNER contract-consumer rule; historical not-proposed disposition retained here |
| `6fdb29d40829b4ec` | Files staged in the shared index from outside any lease | 1 | 3 | F01 | retired as an agent concern (environmental, proven F01); do not re-raise |
| `1f54dec2254e8f15` | A declared serial-lease-handoff line for a source directory should name its paired test-directory handoff | 1 | 4(F03) | F03 | adopted — current PLANNER contract-consumer and shared-assertion rules cover paired test handoffs; no attributable role commit available |
| `1c51a85390a3f964` | A rule a concurrently-dispatched sibling session discovers mid-run has no channel to reach another concurrently-dispatched sibling building against the same shared fact vocabulary | 1 | 1(F03) | F03 | open — receive/recheck and lease rules exist; no live sibling rule-notification channel specified; no F05 recurrence |
| `ed1f65d3f1c1e905` | A payload cached by the projection (M12) and sealed by staging (M23) is the same durable fact encoded twice; a version change must lease both | 1 | 3 | F04 | promoted → PROGRAM-CONFIG Conventions at F04; carried unchanged |
| `242f2bb3c9d6af33` | A checkpoint's seed instruction should name the exact producing function, not just the file | 1 | 1(F04) | F04 | open — current rules require authoritative seeds but do not explicitly require exact seed-function references |
| `abf5cf9b579e46b5` | A worker-tier proving assertion requires exact counts, not an inequality, at the first checkpoint that proves a capability | 1 | 1(F04) | F04 | open — exact-count requirement not explicit in current generic proof rules; F05 plans do use exact assertions |
| `941f0fdbdd02f2c4` | Bounded backup prerequisites emerge one per return instead of one complete producer-consumer trace | 1 | 4(F05: S02 r1/r2/r3/r4) | F05 | promoted — arch/PROGRAM-CONVENTIONS.md; framework recommendation open; external config owner preserved |
| `52cdcf97581fa215` | Receive updates current tables but leaves present-tense producer summaries at an older revision | 1 | 1(F05 final receive; two summary locations) | F05 | open — extend existing CA/CAP receive invariant to present-tense inheritance and preflight summaries |

## 2026-09-25 — durable-home-backup (F05), continuation final, local/bundle accepted; overall incomplete

### Record, scope and preserved ownership

One final pass under `.program/prompts/ARCHIVIST-F05-CONTINUATION-FINAL.prompt.md`, no delegation. Read the current report with its raw followUp/surprises and carried dispositions, STATE in full including historical returns, every arch file including protected seeds, PROGRAM-CONFIG, MASTER/all seven sessions, the previous Archivist entry and F02/F03/F04 final log entries. Read current role recommendation clauses and checked exact-path tracking/history. Inspected git through envelope base `7743353`, production `47a633b`, relevant approved database sections/migrations, source producers/consumers and sampled boundary assertions. No application test was rerun; recorded executable results remain attributed to their workers/root. The write set is tracked architecture, this append-only log and cleanup ledger only.

The previous final pass `5959ca4` is historical, not rewritten. External config/roadmap, seven untracked arch seeds, all role documents, report, STATE, plans, protected specs and implementation are byte-preserved. M05/M47 implementation is reconciled in their already tracked `*-f05-delta.md` files. No new architecture file or role was introduced. PROGRAM-CONFIG remains its owner's file; crossed-threshold convention wording is promoted now in owned PROGRAM-CONVENTIONS.

### Reconciled current contracts

S01/S02/S03 are accepted 3/6/4 checkpoints. Folded S02 full-save/mounted/reset/recovery/r10 and S03 command/policy/reminder/status deltas into module-specific current contracts, removing repeated mixed-module paragraphs and superseded pending/failed-receive summaries. M07/M51/M53 now describe verified fallback delivery and operation-bound explicit confirmation without expanding the durable terminal union. M27/M33 distinguish vault-only supported graph recovery from future nonempty writers/adoption. M32 owns wire contracts, M33 owns authenticated readers/transactions, M36 owns actor lifetimes, M37 owns selectors and M41/M42/M44/M46/M47/M54 own their actual surfaces/composition. M56/M60/M61 now distinguish component, decoder/browser and real-entry proof. M59's empty KAT fixture no longer leaves current application recovery incorrectly pending.

Closed inherited CAP-05 countdown in M36/M37/M41/M54, without treating UI expiry as authority. Corrected M41's anticipated schema re-entry: current scoped vault rows use landed encrypted state without migration changes. Corrected M42's broad absent-backup claim: per-app bundle status/remedy is mounted, while global empty-library connection/adoption remains a named F06 obligation. S03 preserved-value copy derives from actual provenance and does not fabricate legacy facts.

Rebuilt all 65 registry rows from TypeScript ASTs of every registered module's non-test JS/TS files: type-only clauses/exports removed, runtime re-exports/dynamic imports included, relative targets resolved and worker URL construction separate. New source M05/M47 is real; runtime edges include M33→M05, M37→M05, M27→M24, M42→M37 and M54→M47. Symbol-reference searches were used separately for cleanup, never to infer edges.

### Agreement review, counterevidence and owner routing

Reviewed CA-34–41 / CAP-05,39–45 against source and current dispositions. Sampled save operation/app/home/artifact correlation, captured frontier nonregression and app-scoped receipt writes; all-device pending count (`2cd5ad4`); vault-only reader closure; current-graph unsupported-branch rejection; same-transaction authored reminder trigger; worker-clock/stale-dismissal guards; schema/chart no-op validation and unchanged durable effects after reopen. Source `823012b` shares hydration/visit writes and closes the r9 import counterexample, with failing-before regressions and fresh real-browser acceptance. Old passing evidence was not used to waive it. No additional source-backed counterexample was established in these bounded samples; this is not an exhaustive implementation audit or a substitute for the owning proof.

Current independent report evidence at `47a633b`: typecheck/lint exit 0; 223 files / 2461 unit pass / 3 inherited skips; fresh-build 11 browser/e2e pass / zero retry or skip. The 81-test full e2e pass is Coder-run on source-identical code, digests inspected by root, not independently rerun in full. No OS-native durability, live vendor qualification or demo verdict is claimed.

**Machine-actionable STATE correction for Orchestrator:** Decisions DEC-71/72, VB-02, CA-37 opening prose, CA-40 countdown prose and Seam Preflight still call accepted local producers/proofs planned or missing. The current CA/CAP tables and receives correctly cite `823012b`/`47a633b`. Graph correction's resumption text still instructs full CP3 resumption and groups provider design with residual inputs. Refresh current prose or label its historical base, preserving raw handoffs. Affected consumers: J1/J2, reset and S07 composition. Smallest correction is documentation-only by Orchestrator; closure is agreement between present-tense summaries and actual accepted producers/proofs, with future graph/cloud/J6 still unverified. Exact source locations and correction route are consolidated in `arch/F05-boundaries.md`. STATE/report/plans are untouched.

Required future work remains fully owned: exact protected typed retained-reference/conflict/audit/chain format from DB/Author → S06 CP1 writer/reader/publication, CP2 two-compaction/install/history/restore and CP4 J3/J1 → S07 consumption. User approval settled direction, not fields/version; no protected worker or edit occurred. Public registrations/redirects/accounts from external supplier → S04/S05 qualification → S07 actual config/token/egress/scheduler/J4/J5. S07 CP1/CP5 credential-free local J6 remains mandatory even without live accounts. Provider design `98ee79c` is accepted, not a remaining design gap. GATE-F05 still needs completed demo and human verdict. F06 Planner owns conservative pin recovery/adoption and inherited empty-library copy; next Planner UI-maintenance owns M46 600px/M39 900px layout debt. AR-1 missing-source grammar validation remains Orchestrator/Archivist debt. These are existing owned obligations, not new session plans.

### Recurrence and convention promotion

The report records **eight** unplanned planning/ownership corrections: initial preflight, three S02 mechanical lease seams, graph replan, worker-clock replan, five-spec compatibility amendment, and six-path no-op producer amendment. The existing complete-prerequisite-trace pattern `941f0fdbdd02f2c4` now has **1 cycle / 7 specific corrections**, excluding the broader initial preflight to avoid conflating its five findings with these instances. S02 r1–r4 are the original four; continuation `37303ce`, `50aed81`, `0634e89` add three. They cross the in-cycle bar independently, so the convention is extended at this pass, not queued behind incomplete F05.

Exact added wording in `arch/PROGRAM-CONVENTIONS.md`:

> When a capability promises no-op suppression, post-acknowledgement behavior or time-based persistence, trace every accepted mutation family through its command producer, atomic durable write, response mapping and mounted consumer. Lease stale/invalid/no-change guards and all affected fixtures/specs with the correction. Name the actual authority realm and executable clock/control mechanism, its behavioral self-tests, restart isolation and artifact identity before the first dependent proof. Adapt existing journeys at their accepted-action boundaries without suppressing the new behavior globally or dropping their assertions. Batch all visible prerequisites before redispatch.

This specializes the existing preflight; it adds no review stage or approval flow. S02 r7 context terminal after CP3, r8 execution shortfall, r9 gate failure corrected by r10 and S03 CP0 ownership return are distinct process events. No checkpoint re-slice/budget was imposed. Transport await timeouts are not crashes. Counts for inherited patterns are held when no new instance was established; the two final passes remain **one F05 cycle**.

### Proposed for the framework and adoption check

- `941f0fdbdd02f2c4`: specialize complete-prerequisite tracing to bounded payload/lifetime paths, every no-op mutation producer, actual time-authority realm/harness self-tests and affected old journeys. **1 cycle, 7 instances.** General producer/consumer/harness rules already exist; this recommendation is a concrete checklist refinement, not a claim they are absent.
- `52cdcf97581fa215`: extend the received-status consistency check to present-tense prose in decisions, baseline, CA introductions and preflight, or explicitly date them. **1 cycle, 2 final-receive observations.** Tables were updated while current prose remained stale. This framework recommendation needs no three-cycle threshold; it is not yet a separate permanent convention.
- Carry the full remaining open backlog below. Current PLANNER/CODER/UI-CODER/ORCHESTRATOR clauses were checked for substance; exact role paths remain untracked and `git log --` yields no attributable adoption commit. Existing paired-consumer, receive-status and verification-maintenance recommendations remain adopted. Narrow table-shape, unleased-module fragment, live sibling notification, exact seed-function and exact-count requests remain absent; general proof language is not their exact adoption. ORCHESTRATOR still uses the 16-session interim threshold and its standard template omits cleanup-ledger ownership. The supplied F05 envelope includes the ledger, so that omission does not gain an instance here. No adoption claim was inferred from byte preservation.

### Cleanup and documentation verification

Updated existing CL-01–06 without duplicating candidates: CL-01/04 retired, CL-02/06 tracking, CL-03/05 briefed/unaccepted. Fresh AST exported-value inventory plus outside-file whole-word source/test search found **212** declarations without textual consumers (initial F05: 211); first twenty witnesses remain. Absence is high-confidence under that method, removal confidence low because internal/public/dynamic/future contracts still need classification. The repeated-ledger-pattern threshold retains the same **Prove or name every unconsumed export** Planner brief, with focused boundary/KAT/worker/recovery checks and `pnpm verify` for any subsequently approved implementation. Pins are intentionally retained, not cleanup targets; provenance closure and inherited layout follow-ups are classified with their existing owners. No source cleanup or fresh bundle-byte measurement occurred.

Documentation verification: re-read changed sections/diff; checked resolved registry witnesses and 65-row coverage; checked relative document links and current source symbols; checked `git diff --check`; carried every inherited standing ID verbatim and preserved the previous log prefix. Compared the 1022-file initial hash inventory: exactly 25 authorized documentation paths changed; all 20 protected external hashes match. Source/tests/specs/plans/STATE/report/roles/seeds/config/roadmap remain byte-preserved. Use explicit owned paths on both add and commit; unrelated changes remain visible and are not claimed clean.

### Standing recommendations

| id | pattern | cycles | in-cycle instances | first seen | status |
| --- | --- | ---: | ---: | --- | --- |
| `27bf4a350ec2f23c` | A dependency must-not ships as a test, not a review note | 4 | 5(F01)+3(F02)+5(F03)+3(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `33dba11758afa4e2` | Encode a must-not as a type or a runtime throw | 4 | 5(F01)+8(F02)+3(F03)+2(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `3e30962a53a57c2f` | Test filters take no bare `--` | 1 | 3 | F01 | promoted → PROGRAM-CONFIG Conventions; no F02/F03/F04 instance |
| `f279893d1ad0a40d` | `Owns` authoritative, Files table indicative | 4 | 7(F01)+9(F02)+12+(F03)+1(F04) | F01 | promoted → Custom Rule 7; F04's continuing mirror-direction instance recorded |
| `3ddf907567cd76e6` | Arch fragment head contract superseded by its own delta | 5 | 4(F01)+6(F02)+6(F03)+1(F04)+9(F05) | F01 | promoted — existing fragment policy; initial six F05 heads plus continuation M36/M41/M42 reconciled |
| `b73d9bab7c196038` | Lease or boundary sweep drawn over the wrong tree | 2 | 3(F02) | F01 | promoted at F02 interim → PROGRAM-CONFIG Conventions; held — 0 new instances F03 or F04 |
| `d7d81c3b2cf8e9c9` | Closed union and its exhaustive map split across leases | 2 | 3(F02)+0(F03)+4(F04) | F02 | promoted → PROGRAM-CONFIG Conventions; F04 mixed result — one blocked (S04), three pre-empted after (S05, S06, S08) |
| `f823e9a910b0a671` | A delta describing another module | 4 | 4(F01)+4(F02)+0(F03)+1(F04)+3(F05) | F01 | promoted — existing fragment policy; initial M56/M57 plus S02 r9 and S03 mixed-module receive sets separated |
| `442cb40af6e1a830` | No interim drift check scheduled below 16 sessions | 5 | 4(F01)+10(F02)+5(F03)+2(F04)+3(F05) | F01 | open — cadence still size-based; F05 three named stale seams, mitigated by two scoped planning reviews |
| `f1442f23b7f0949d` | Archivist envelope write set omits the cleanup ledger it is asked to own | 4 | 1(F01)+2(F02)+1(F03)+1(F04) | F01 | open — framework template still omits ledger; this F05 envelope explicitly includes it, no new instance |
| `b06241fe8526baa2` | Landed proof legs left in STATE's `planned:` list at receive | 1 | 13 | F02 | **adopted** — ORCHESTRATOR.md's receive step 7 CA/CAP status invariant; F04 shows zero recurrence for the second cycle running |
| `b027c25ef168decb` | STATE table rows that lose a cell go unnoticed | 1 | 6(F02) | F02 | open — no new F05 malformed-row finding; current role lacks a table-shape check |
| `06e04ece038e736d` | A module no session leased has no route into its own fragment | 1 | 6(F02) | F02 | open — no new F05 instance; generic fragment integration does not specify unleased-module routing |
| `3261d808a9c412da` | FORGE-CONFIG Verification Commands has no post-baseline maintainer | 1 | 2 | F01 | adopted (rule + supersession text exist); third consecutive cycle of the expected maintenance note, not a recurrence of the original gap |
| `1a8974acd0d1350e` | Checkpoint boundary blur: capability bodies and proofs split across checkpoints | 2 | 2(F01)+0(F02) | F01 | retired — settled negative at F02; no F03 or F04 instance either (32+31+35 checkpoints across three cycles, no sizing failure) |
| `26e3bdf9a1e6dd6f` | Authorized contract change breaks a consumer outside the lease | 1 | 1 | F02 | adopted — substance already in PLANNER contract-consumer rule; historical not-proposed disposition retained here |
| `6fdb29d40829b4ec` | Files staged in the shared index from outside any lease | 1 | 3 | F01 | retired as an agent concern (environmental, proven F01); do not re-raise |
| `1f54dec2254e8f15` | A declared serial-lease-handoff line for a source directory should name its paired test-directory handoff | 1 | 4(F03) | F03 | adopted — current PLANNER contract-consumer and shared-assertion rules cover paired test handoffs; no attributable role commit available |
| `1c51a85390a3f964` | A rule a concurrently-dispatched sibling session discovers mid-run has no channel to reach another concurrently-dispatched sibling building against the same shared fact vocabulary | 1 | 1(F03) | F03 | open — receive/recheck and lease rules exist; no live sibling rule-notification channel specified; no F05 recurrence |
| `ed1f65d3f1c1e905` | A payload cached by the projection (M12) and sealed by staging (M23) is the same durable fact encoded twice; a version change must lease both | 1 | 3 | F04 | promoted → PROGRAM-CONFIG Conventions at F04; carried unchanged |
| `242f2bb3c9d6af33` | A checkpoint's seed instruction should name the exact producing function, not just the file | 1 | 1(F04) | F04 | open — current rules require authoritative seeds but do not explicitly require exact seed-function references |
| `abf5cf9b579e46b5` | A worker-tier proving assertion requires exact counts, not an inequality, at the first checkpoint that proves a capability | 1 | 1(F04) | F04 | open — exact-count requirement not explicit in current generic proof rules; F05 plans do use exact assertions |
| `941f0fdbdd02f2c4` | Capability prerequisites emerge per return instead of a complete producer/consumer/proof trace | 1 | 7(F05: S02 r1–r4; clock, compatibility, no-op corrections) | F05 | promoted — arch/PROGRAM-CONVENTIONS.md extended now; framework specialization open; external config preserved |
| `52cdcf97581fa215` | Receive updates current tables but leaves present-tense producer summaries at an older revision | 1 | 2(F05 initial and continuation final receives) | F05 | open — cover prose CA summaries, decisions, baseline and preflight; current tables already accepted |

## 2026-09-25 — durable-home-backup (F05), S06 compaction final, CAP-44 verified; overall incomplete

### Record, scope and preserved ownership

Final pass reconciling the S06 compaction continuation: input `FINAL-REPORT.md` at `53f7c73` (top "This continuation — S06 compaction (CAP-44)" section; S06 accepted at `2d8ff2d`). Read `program-agents/ARCHIVIST.md` in full, STATE.md in full (2367 lines, read in sections — Program/Session Status/Wave/Dependency/Architecture/Scope/Decisions/Approval/Verification-Baseline/Capability-Readiness/Contract-Agreements/Seam-Preflight/Executable-Proof-Specs/Current-Blockers/Accepted-graph-reconciliation/Granularity through the newest S06 r2–r9 handoff sections, and the section-header index for the rest), every file under `arch/`, PROGRAM-CONFIG.md (read, not edited — protected per envelope), and both prior ARCHIVIST-LOG.md entries above. Cross-checked `git log` for S06's commit sequence (`9f0ef04`..`53f7c73`) and the 7-file arch delta `71e3adc`. No application test was rerun; recorded executable results (typecheck/lint/unit/browser counts at `2d8ff2d`) remain attributed to Orchestrator's independent receive, cited from STATE/report, not re-executed here. The write set is tracked architecture (`arch/**`), this append-only log and the cleanup ledger only — `PLANNER.md`/`CODER.md`/`UI-CODER.md`/`ORCHESTRATOR.md`, `STATE.md`, `MASTER.md`, the Final Report, session files, specs, migrations and implementation source are byte-preserved. PROGRAM-CONFIG.md's own uncommitted outside edits were read, not staged or committed, per the envelope's explicit instruction.

Both prior log entries are historical and were reproduced verbatim above this section, not rewritten (Principle 2's "reconcile, don't append" governs `arch/`, not this append-only log itself). The pre-existing untracked M05/M25/M26/M29/M30/M47/M64 arch seeds named in the envelope were not touched.

### Reconciled current contracts

Folded the seven `<!-- durable-home-backup SESSION-06 r9 -->` stapled deltas (`71e3adc`) into their owning modules' existing "current contract"/"Durable-home implementation" sections, rather than leaving them as trailing raw blocks superseding the prose above them (the standing `3ddf907567cd76e6` pattern):

- **M09-codecs.md** — folded `encodeEventProvenance`/`decodeEventProvenance` and the manifest audit/conflict logical-order contract into the existing Public API and Durable-home sections; updated the Source line through `2d8ff2d`.
- **M12-projection.md** — folded the new `checkpoint-export.ts`/`evidence-events.ts`/`checkpoint-history.ts` internal files into the Internal-modules table (file count 16 → 19, cross-checked against `git log --diff-filter=A`, all three landed in `084ecf9`) and the original-evidence replay contract into a new "S06 compaction" subsection; moved the nonempty-graph "Known gaps" row to closed, since S06 landed exactly what it named as awaiting.
- **M23-staging.md** — folded discriminated `AppHeadV1|V2`/`RecordPageV1|V2` roots, the `compaction` cleanup reason and `beforeCompactionBatch` gate into a new "S06 compaction" section, cross-referencing rather than duplicating the M33/M09 material.
- **M24-sync-protocol.md** — folded `verifyBackupFrontier`'s new basis-frontier-coverage check directly into the existing `frontier.ts` paragraph, and noted `minimumReaderVersion` stays `1` (no new reader-version gate) rather than leaving that as an unexplained one-line staple.
- **M33-workers.md** — added `data/compaction.ts` (new file; count 22 → 23) to the Landed-structure table, folded backup-graph V2 traversal/event-store/app-session/handlers-scheduler/catalog-reachability material into the existing "Durable-home implementation" section as a new "S06 compaction" subsection rather than a disconnected staple.
- **M37-view-models.md** and **M44-ui-records.md** — the stapled delta was **identical text describing both modules** (`ChangeHistoryVm.scope`/screen copy), the standing `f823e9a910b0a671` pattern recurring. Split it: the VM-value contract (`"retained-history"`/`"no-retained-changes"` scope/emptiness pair) is now M37's own "F05 addition" under `records.ts`; the screen-copy consumption and the " · this device" line's restoration (`fccc345`, correcting an unauthorized removal S06 CP2 had introduced) is now M44's own new "Retained history" section, cross-referencing M37 instead of repeating its VM facts.

`MODULE-REGISTRY.md` was re-derived by the same AST-scan methodology as the prior passes (never by symbol grep for edges): M12's count corrected 16 → 19 and M33's 22 → 23, both confirmed by `git log --diff-filter=A` on the four new files landing in `084ecf9`. Every value import in those four files resolves to a module already in that file's owning module's existing edge set — **no new module-level edge was created**, only new files inside already-declared edges; this was verified by reading the new files' import statements directly, not inferred from the delta prose (the standing M17→M02/`requireGitRepo` discipline this registry has followed since F02).

`F05-boundaries.md` was rewritten (not appended to) so CA-35/CAP-40 and CA-41/CAP-44 read "landed and independently verified" rather than the prior "producer/proofs remain S06/planned" phrasing that is now stale relative to `2d8ff2d`; the "Sources, actual composition and proof limits" and "Carried follow-ups" sections were updated the same way. This is the arch-level analogue of the STATE-prose drift the previous two passes flagged and fixed — see "Current-summary drift", below.

### Agreement review, counterevidence and owner routing

Sampled STATE's Contract Agreements (CA-35/36/40/41) and Capability Readiness (CAP-44) rows against the report's independent-receive evidence at `2d8ff2d` and the S06 r2–r9 handoff narrative: candidate+installed selector 39/473, full unit 229/2520/3 skips, browser J3 2/2 (two real 128-tail periodic compactions, same-context restart, retained-history restore, V2 edit+append, vault-only oracle equivalence for every artifact), sync/compaction+bundle 3/3, and the remaining 18/18 regression suite — 23/23 total, matching a build tagged `2d8ff2d`. Sampled the CP1 counterexample's root cause (`7e785e4`): a test fixture picked a random demo row that sometimes carried validator warnings while hard-coding an empty evidence report, so `evidence-events.ts`'s `requireReport` correctly refused it; production's refusal was not loosened, the fixture was corrected to cover both clean and pinned-warned subjects plus an explicit empty-report negative control. This is a verification gate working as designed, not a counterexample against CA-35/41's evidence.

No source-backed counterexample against any inherited verified CA/CAP row was found in this bounded sample. STATE's own record (S06 r9 receive) already distinguishes the accepted quota disposition (component-level QuotaExceededError proof accepted for CP3 because Chromium ignores the CDP quota override — real-browser quota refusal is named final-report debt, not silently waived) from the `postCheckpointCommitCount`/gate/scheduler evidence proper; this pass did not need to invent that distinction, only preserve it in `M33-workers.md`/`F05-boundaries.md`.

**Machine-actionable STATE correction for Orchestrator: none new this pass.** The prior pass's corrections (DEC-71/72, approval-review disposition, VB-02, CA-37/40 prose, Seam Preflight) were re-inspected against the current STATE text at receive `9d72cf7` and confirmed already resolved — every relevant DEC/CA/Seam-Preflight row already cites `47a633b`/`823012b`/`2d8ff2d` in the correct tense. Capability Readiness's CAP-44 row already reads "verified"; Contract Agreements' CA-35/36/40/41 rows already read "agreed" citing `2d8ff2d`; Current Blockers' PLAN-REVIEW-GRAPH-APPROVED and GRAPH-CONTRACT rows already read "cleared"/"closed" in correct past tense. This is itself the notable finding of this pass: the receive-time stale-prose pattern (`52cdcf97581fa215`) did **not** recur at this second consecutive receive (see "Recurrence", below).

### Recurrence and convention promotion

The Final Report's own Process-effectiveness section gives exact evidence for a **third recurrence, within the same F05 cycle, of the complete-prerequisite-trace pattern** (`941f0fdbdd02f2c4`): S06's own first three attempts (r2, r3, r4) each found a lease-completeness gap before any CP1 implementation checkpoint landed — the vault-manifest-codec ownership gap (r2, found by scoped Archivist review `Adw4M`, corrected `baf4b56`), the history-copy consumer seam (r3), and the exact runtime-fixture gap (r4, lease amendment before instruction r5) — the identical shape as S02 r1–r3 at the start of F05's very first working set. These three are counted; S06's later r5/r7 "missing Author mapping" returns are **not** counted, because `DEC-75` explicitly designates DB/Author re-entry as the expected mechanism for discovering a new durable-constraint detail during implementation of a deliberately incremental graph-contract format — conflating a designed re-entry with a mechanical lease-completeness gap would overstate the pattern. r6's context exhaustion and r8's provider-usage-limit terminal are environment events, not planning defects, matching the same exclusion rule the prior two passes already applied to S02 r7/r8.

Updated count: **1 cycle (F05), 10 in-cycle mechanical-prerequisite instances** (S02 r1–r4: 4; continuation clock/compatibility/no-op: 3; S06 CP1 r2–r4: 3). The convention text in `arch/PROGRAM-CONVENTIONS.md` was extended (not re-promoted — it was already promoted at the prior pass) with a new dated section recording this third recurrence and its exact commit evidence, per Principle 4's instruction not to defer a met threshold to a later pass. The Final Report's own granularity feedback additionally flags CP1 as a single very-large (44-file) checkpoint that a future graph-contract session should split (codec/candidate vs. evidence replay); this is recorded as supporting context for *why* a large first checkpoint accumulates repeated gaps, not as an independent standing pattern of its own.

### Proposed for the framework and adoption check

Two new observations this pass, neither below any threshold (Principle 3 has none):

- **`8ad00fb101eef379` — a checkpoint's prescribed verification selector can name a test file that does not exist in the tree.** S06's Verification named `tests/unit/queries/history.test.ts`; the actual repository has no such file (history's tests live in `tests/unit/queries/records.test.ts`, confirmed by directory listing). The gap was caught and substituted at receive (`2d8ff2d`, "with `tests/unit/queries/records.test.ts` substituted for the non-existent `tests/unit/queries/history.test.ts`"), so it cost a receive-time correction, not an acceptance failure — but it is the same class of gap `242f2bb3c9d6af33` already named for exact seed-function references. Recommend Planner check a checkpoint's named test paths against the actual tree (a single `ls`/`find`) before finalizing a lease, the same discipline already expected of production source paths. **1 cycle, 1 instance.**
- **`0112e369bdd88b60` — a deliberately incremental protected-format plan does not say upfront how many Author re-entries it expects.** The accepted graph contract (`afe37f3`) was followed by three further DB/Author commits (`91ad694`, `17a73ae`, `8c303fd`) discovered only once S06 CP1 implementation began (r5, r7) — by design, per `DEC-75`, and correctly excluded from the lease-completeness count above, but nothing in the plan told Orchestrator or a reader in advance that roughly this many re-entries were the expected shape of "the format is intentionally incremental." Recommend that when Planner deliberately defers exact protected-format detail to implementation-time DB/Author re-entry, it say so explicitly and give a rough expected cadence, so a reader does not have to reconstruct after the fact (as this log now does) that repeated re-entry was the designed mechanism rather than a planning gap. **1 cycle, 2 instances (r5, r7).**

Adoption check: read current `PLANNER.md`/`CODER.md`/`UI-CODER.md`/`ORCHESTRATOR.md` for the substance of every open carried item and re-ran `git log --`/`git ls-files --` on those exact paths — **still not tracked in this repository**, so there is still no attributable role commit to cite for dating; this is the third consecutive pass with that same limitation, not a new finding. No open item's substance was found newly present in the current role text. `941f0fdbdd02f2c4` and `52cdcf97581fa215`'s specializations remain open framework recommendations, now with updated evidence (above). No adoption claim was inferred from byte preservation — the role files are unchanged because this pass wrote nothing to them, which proves only that Archivist behaved, not that a human declined to act (per the operating principle distinguishing these two claims).

### Cleanup and documentation verification

Updated `CLEANUP-LEDGER.md` without duplicating candidates: CL-01/04 retired, CL-02/06 tracking, CL-03/05 remain briefed. Did **not** repeat the full AST-plus-whole-word-search methodology a third time in one cycle (the last full scan, 212 declarations, remains the continuation-final pass's evidence); instead ran a narrower whole-word `grep` spot check limited to S06's four new files, which found three candidates (`postCheckpointCommitCount`, `COMPACTION_TAIL_THRESHOLD`, `COMPACTION_INTERVAL_MS`) with no outside-file textual match — recorded explicitly as weaker, non-full-methodology evidence with a named false-positive risk (`DataWorkerDependencies.compaction`'s object-shaped test-override point, per `M33-workers.md`), not folded into the 212 count and not added to the classification brief's target set ahead of its next full re-scan. The standing **Prove or name every unconsumed export** brief is otherwise unchanged; it is not removal authority.

Re-read every rewritten file after writing, checked cross-file consistency (M09/M12/M23/M24/M33/M37/M44 cite each other correctly for the split M37/M44 material; `MODULE-REGISTRY.md`'s file counts match `git log --diff-filter=A` for the four new files; `F05-boundaries.md`'s CA/CAP table matches STATE's current rows at `9d72cf7`). Checked `git diff --check` is not applicable (no code diff); confirmed `PLANNER.md`/`CODER.md`/`UI-CODER.md`/`ORCHESTRATOR.md`, `STATE.md`, `MASTER.md`, the Final Report, `PROGRAM-CONFIG.md` and `ROADMAP.md` are untouched by this pass (not staged, not committed). The seven pre-existing untracked arch seeds named in the envelope were not read for edit purposes and are not part of this commit's pathspec.

### Standing recommendations

| id | pattern | cycles | in-cycle instances | first seen | status |
| --- | --- | ---: | ---: | --- | --- |
| `27bf4a350ec2f23c` | A dependency must-not ships as a test, not a review note | 4 | 5(F01)+3(F02)+5(F03)+3(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `33dba11758afa4e2` | Encode a must-not as a type or a runtime throw | 4 | 5(F01)+8(F02)+3(F03)+2(F04) | F01 | promoted → PROGRAM-CONFIG Conventions |
| `3e30962a53a57c2f` | Test filters take no bare `--` | 1 | 3 | F01 | promoted → PROGRAM-CONFIG Conventions; no F02/F03/F04 instance |
| `f279893d1ad0a40d` | `Owns` authoritative, Files table indicative | 4 | 7(F01)+9(F02)+12+(F03)+1(F04) | F01 | promoted → Custom Rule 7; F04's continuing mirror-direction instance recorded |
| `3ddf907567cd76e6` | Arch fragment head contract superseded by its own delta | 5 | 4(F01)+6(F02)+6(F03)+1(F04)+16(F05) | F01 | promoted — existing fragment policy; F05 now includes this pass's M09/M12/M23/M24/M33/M37/M44 reconciliation (+7) atop the prior 9 |
| `b73d9bab7c196038` | Lease or boundary sweep drawn over the wrong tree | 2 | 3(F02) | F01 | promoted at F02 interim → PROGRAM-CONFIG Conventions; held — 0 new instances F03–F05 |
| `d7d81c3b2cf8e9c9` | Closed union and its exhaustive map split across leases | 2 | 3(F02)+0(F03)+4(F04) | F02 | promoted → PROGRAM-CONFIG Conventions; F04 mixed result — one blocked (S04), three pre-empted after (S05, S06, S08) |
| `f823e9a910b0a671` | A delta describing another module | 4 | 4(F01)+4(F02)+0(F03)+1(F04)+4(F05) | F01 | promoted — existing fragment policy; this pass's M37/M44 identical-delta split (+1) added to the prior 3 |
| `442cb40af6e1a830` | No interim drift check scheduled below 16 sessions | 5 | 4(F01)+10(F02)+5(F03)+2(F04)+3(F05) | F01 | open — cadence still size-based; no new F05 instance this pass (final pass, not interim) |
| `f1442f23b7f0949d` | Archivist envelope write set omits the cleanup ledger it is asked to own | 4 | 1(F01)+2(F02)+1(F03)+1(F04)+1(F05) | F01 | open — this pass's envelope explicitly listed `arch/`, `ARCHIVIST-LOG.md`, `PROGRAM-CONFIG.md` and `STATE.md` but not `CLEANUP-LEDGER.md`; reconciled it anyway under the role's default write set (+1 instance, prior F05 continuation-final envelope had explicitly included it) |
| `b06241fe8526baa2` | Landed proof legs left in STATE's `planned:` list at receive | 1 | 13 | F02 | **adopted** — ORCHESTRATOR.md's receive step 7 CA/CAP status invariant; zero recurrence through F05's S06 receive (`9d72cf7` already correctly marks CAP-44 verified) |
| `b027c25ef168decb` | STATE table rows that lose a cell go unnoticed | 1 | 6(F02) | F02 | open — no new F05 malformed-row finding; current role lacks a table-shape check |
| `06e04ece038e736d` | A module no session leased has no route into its own fragment | 1 | 6(F02) | F02 | open — no new F05 instance; generic fragment integration does not specify unleased-module routing |
| `3261d808a9c412da` | FORGE-CONFIG Verification Commands has no post-baseline maintainer | 1 | 2 | F01 | adopted (rule + supersession text exist); fourth consecutive cycle of the expected maintenance note, not a recurrence of the original gap |
| `1a8974acd0d1350e` | Checkpoint boundary blur: capability bodies and proofs split across checkpoints | 2 | 2(F01)+0(F02) | F01 | retired — settled negative at F02; no F03/F04/F05 instance (S06's 4 checkpoints plus 4 verified corrections show no sizing failure at the capability-boundary level; CP1's size is a separate granularity note, above) |
| `26e3bdf9a1e6dd6f` | Authorized contract change breaks a consumer outside the lease | 1 | 1 | F02 | adopted — substance already in PLANNER contract-consumer rule; historical not-proposed disposition retained here |
| `6fdb29d40829b4ec` | Files staged in the shared index from outside any lease | 1 | 3 | F01 | retired as an agent concern (environmental, proven F01); do not re-raise |
| `1f54dec2254e8f15` | A declared serial-lease-handoff line for a source directory should name its paired test-directory handoff | 1 | 4(F03) | F03 | adopted — current PLANNER contract-consumer and shared-assertion rules cover paired test handoffs; no attributable role commit available |
| `1c51a85390a3f964` | A rule a concurrently-dispatched sibling session discovers mid-run has no channel to reach another concurrently-dispatched sibling building against the same shared fact vocabulary | 1 | 1(F03) | F03 | open — receive/recheck and lease rules exist; no live sibling rule-notification channel specified; no F05 recurrence (S06 was the sole eligible session this run, so no sibling scenario arose) |
| `ed1f65d3f1c1e905` | A payload cached by the projection (M12) and sealed by staging (M23) is the same durable fact encoded twice; a version change must lease both | 1 | 3 | F04 | promoted → PROGRAM-CONFIG Conventions at F04; carried unchanged — no new F05 instance (S06's V2 roots landed in both M23's `roots.ts` and M12 together, in-lease, no drift) |
| `242f2bb3c9d6af33` | A checkpoint's seed instruction should name the exact producing function, not just the file | 1 | 1(F04) | F04 | open — current rules require authoritative seeds but do not explicitly require exact seed-function references; see the related new `8ad00fb101eef379` below for this cycle's test-path variant of the same underlying gap |
| `abf5cf9b579e46b5` | A worker-tier proving assertion requires exact counts, not an inequality, at the first checkpoint that proves a capability | 1 | 1(F04) | F04 | open — exact-count requirement not explicit in current generic proof rules; S06's J3 used exact assertions (127/128/129/257) consistent with the recommendation, but that is F05 plan practice, not role-document adoption |
| `941f0fdbdd02f2c4` | Capability prerequisites emerge per return instead of a complete producer/consumer/proof trace | 1 | 10(F05: S02 r1–r4: 4; continuation clock/compatibility/no-op: 3; S06 CP1 r2–r4: 3) | F05 | promoted — arch/PROGRAM-CONVENTIONS.md extended a third time this pass with S06's evidence; framework specialization open; external config preserved |
| `52cdcf97581fa215` | Receive updates current tables but leaves present-tense producer summaries at an older revision | 1 | 2(F05 initial and continuation-final receives); **0 new instances at this S06-compaction receive** | F05 | open — second consecutive receive (`9d72cf7`) with no recurrence; the discipline is holding without yet being promoted to a role document |
| `8ad00fb101eef379` | A checkpoint's prescribed verification selector names a test file that doesn't exist in the tree | 1 | 1(F05: S06 `tests/unit/queries/history.test.ts`, substituted at receive) | F05 | open — new this pass; proposed for the framework, above |
| `0112e369bdd88b60` | A deliberately incremental protected-format plan does not state upfront how many Author re-entries it expects, so repeated legitimate re-entry looks like a planning gap in retrospect | 1 | 2(F05: S06 r5, r7 — by-design per DEC-75, explicitly not counted as a lease-completeness defect) | F05 | open — new this pass; proposed for the framework, above |
